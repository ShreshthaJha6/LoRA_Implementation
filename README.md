# LoRA Reproduction

A compact, reproducible comparison of frozen-feature and LoRA adaptation for `distilbert-base-uncased` on GLUE SST-2.

Run the notebook in `notebooks/` on Kaggle to execute the E0, E1 (rank 4), and E2 (rank 8) experiments.
